name = "gait:log"
description = "Show recent GAIT commits (short IDs, message, parents, turn counts)."

prompt = """
Important:
- Do NOT call gait_record_turn() after executing this tool.
- Respond with only the tool output summary. Do not say "committed", "recorded", or "saved".
Call gait_log(limit=20) and show a readable summary:
- commit, created_at, kind, parents, turns, message
If the user asks for more, increase limit.
"""
