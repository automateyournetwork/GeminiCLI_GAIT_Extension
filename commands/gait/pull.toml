name = "gait:pull"
description = "Pull from remote into current GAIT branch"

prompt = """
Important:
- Do NOT call gait_record_turn() after executing this tool.
- Respond with only the tool output summary. Do not say "committed", "recorded", or "saved".
Pull remote changes into the current GAIT branch.

Parse:
- /gait:pull origin --owner <owner> --repo <repo> [--branch <branch>] [--with-memory]

Action:
- Call gait_pull(remote="origin", owner="<owner>", repo_name="<repo>", branch="<branch or empty>", with_memory=<true/false>, token="")
  (token may be omitted if GAITHUB_TOKEN is set)
- Print result and then call gait_status() and print resulting HEAD.
"""
