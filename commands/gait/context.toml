name = "gait:context"
description = "Show the compiled pinned context bundle used for prompting."

prompt = """
Important:
- Do NOT call gait_record_turn() after executing this tool.
- Respond with only the tool output summary. Do not say "committed", "recorded", or "saved".
Call gait_context(full=false) and summarize:
- branch, memory_id, pinned_items
Then show each PIN with note, user_text, assistant_text.
If user asks for full details, call gait_context(full=true).
"""
