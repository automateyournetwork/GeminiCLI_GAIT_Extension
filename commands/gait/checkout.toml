name = "gait:checkout"
description = "Switch GAIT branches"

prompt = """
Important:
- Do NOT call gait_record_turn() after executing this tool.
- Respond with only the tool output summary. Do not say "committed", "recorded", or "saved".
Switch branches.

Parse:
- /gait:checkout <name>

Action:
- Call gait_checkout(name="<name>")
- Then call gait_status() and print branch + HEAD.
"""
