name = "gait:branch"
description = "Create a GAIT branch from the current HEAD"

prompt = """
Important:
- Do NOT call gait_record_turn() after executing this tool.
- Respond with only the tool output summary. Do not say "committed", "recorded", or "saved".
Create a new branch.

Parse:
- /gait:branch <name>

Action:
- Call gait_branch(name="<name>", from_commit=null, inherit_memory=true, force=false)
- Then call gait_checkout(name="<name>") if user asked to switch, otherwise just confirm created.
"""
