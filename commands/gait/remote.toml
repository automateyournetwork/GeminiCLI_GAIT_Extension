name = "gait:remote"
description = "Show or set GAIT remotes (typically origin)"

prompt = """
Manage GAIT remotes.

If user provides: /gait:remote origin <url>
- Call gait_remote_add(name="origin", url="<url>")

If user provides: /gait:remote
- Call gait_remote_list(verbose=true)

If user provides: /gait:remote origin
- Call gait_remote_get(name="origin")

Then print a short confirmation.
"""
