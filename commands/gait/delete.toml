name = "gait:delete"
description = "Delete a GAIT branch"

prompt = """
Important:
- Do NOT call gait_record_turn() after executing this tool.
- Respond with only the tool output summary. Do not say "committed", "recorded", or "saved".
Delete a local GAIT branch.

Parse:
- /gait:delete <branch_name>

Rules:
- Refuse if branch is current branch (must checkout another branch first).
- If branch doesn't exist, say so.

Action:
- Call gait_delete_branch(name="<branch_name>")
- Confirm.
"""
